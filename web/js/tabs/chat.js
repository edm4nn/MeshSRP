// Shell placeholder: la pipeline messaggi (cifratura, frammentazione,
// TXPKT/RXPKT reali) è la milestone successiva.

export function mount(container) {
  container.innerHTML = `
    <div class="placeholder">
      <div class="placeholder-title">Chat</div>
      <p>La messaggistica cifrata arriva nella prossima milestone,
      insieme alla pipeline TXPKT/RXPKT sui messaggi reali.</p>
      <span class="placeholder-badge">IN ARRIVO</span>
    </div>
  `;
}
